[sources]
# Read by the source itself (init_replication / replication_source).
pg_legacy_replication.credentials="postgresql://loader:loader@localhost:5432/dlt_data"

# Left commented, as upstream: the tests' src_config fixture reads this key,
# finds nothing, and falls back to the DESTINATION__POSTGRES__CREDENTIALS env
# var that the Makefile sets.
#pg_replication.credentials="postgresql://loader:loader@localhost:5432/dlt_data"
